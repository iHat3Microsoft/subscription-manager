{
  "name": "subscription-manager",
  "version": "1.0.0",
  "description": "Backend that builds Mihomo master configs and proxy-providers from per-user VPN keys (AmneziaWG/Wireguard/VLESS)",
  "main": "src/build.js",
  "scripts": {
    "build": "node src/build.js",
    "test": "node --check src/build.js && node --check src/parsers.js && node tests/test_parsers.js && bash tests/test_shell_helpers.sh"
  },
  "keywords": [
    "mihomo",
    "clash-meta",
    "vpn",
    "amneziawg",
    "wireguard",
    "vless"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "js-yaml": "^4.2.0"
  }
}
